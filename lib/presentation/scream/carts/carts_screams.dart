import 'package:flutter/material.dart';

class CartsScreams extends StatelessWidget {

  static const String name = 'cart';
  const CartsScreams({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Carts')),
    );
  }
}
